import { PageProps } from 'gatsby';
import * as React from 'react';

const NotFound = (props: PageProps) => <h1>Page Not Found!</h1>;

// eslint-disable-next-line arca/no-default-export
export default NotFound;
